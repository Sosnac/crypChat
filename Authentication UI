import React, { useState } from 'react';
import { View, TextInput, TouchableOpacity, Text, StyleSheet, Alert } from 'react-native';

const AuthScreen = () => {
  const [isLogin, setIsLogin] = useState(true);
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');

  const handleAuth = async () => {
    const endpoint = isLogin ? '/login' : '/register';
    // Logic to call your Node.js API
    console.log(`Attempting ${endpoint} for ${username}`);
  };

  return (
    <View style={styles.container}>
      <Text style={styles.logo}>crypChat</Text>
      <TextInput 
        style={styles.input} 
        placeholder="Username" 
        onChangeText={setUsername}
        autoCapitalize="none"
      />
      <TextInput 
        style={styles.input} 
        placeholder="Password" 
        secureTextEntry 
        onChangeText={setPassword}
      />
      {!isLogin && (
        <Text style={styles.warningText}>
          ⚠️ No email recovery. Save your password carefully!
        </Text>
      )}
      <TouchableOpacity style={styles.button} onPress={handleAuth}>
        <Text style={styles.buttonText}>{isLogin ? 'Login' : 'Sign Up'}</Text>
      </TouchableOpacity>
      <TouchableOpacity onPress={() => setIsLogin(!isLogin)}>
        <Text style={styles.toggleText}>
          {isLogin ? "Don't have an account? Register" : "Already have an account? Login"}
        </Text>
      </TouchableOpacity>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 20, backgroundColor: '#fff' },
  logo: { fontSize: 32, fontWeight: 'bold', textAlign: 'center', marginBottom: 40, color: '#075e54' },
  input: { borderWidth: 1, borderColor: '#ddd', padding: 15, borderRadius: 8, marginBottom: 15 },
  button: { backgroundColor: '#075e54', padding: 15, borderRadius: 8, alignItems: 'center' },
  buttonText: { color: '#white', fontWeight: 'bold' },
  warningText: { color: 'red', fontSize: 12, marginBottom: 10, textAlign: 'center' },
  toggleText: { marginTop: 20, textAlign: 'center', color: '#075e54' }
});
